package org.pra.nse;

import org.springframework.context.annotation.PropertySource;

@PropertySource("classpath:pradeep.properties")
//@PropertySource({"classpath:persistence-${envTarget:mysql}.properties"})
public class AppProps {
}
